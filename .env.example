# Base URL for BFF / health API requests (no trailing slash required)
VITE_API_BASE_URL=http://localhost:3000

# Mock auth headers sent to BFF (spec 006 / 008)
VITE_MOCK_USER_ID=u-dev
VITE_MOCK_ROLES=admin
